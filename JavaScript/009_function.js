'use strict';
//define and invoking function
/**
 *     function name(x){
 *     		some sentences;
 *          return ?;
 *     }
 *      
 *     var name=function(x){
 * 			soem centences;
 * 			return ?;
 *     };
 * 
 * check parameter
 * if(typeof X!=='number'){
 * 		throw 'not a number';
 * }
 * 
 * arguments.length is used to judge the number of parameters 
 * 
 * ES6:   ...rest parameter
 */
function sum(...rest){
	
}